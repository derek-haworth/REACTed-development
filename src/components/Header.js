import React from "react";

const Header = () => (
    <header>
        <h3 className="text-center">Click on any image to begin, but don't click on one more than once!</h3>
    </header>  
);

export default Header;